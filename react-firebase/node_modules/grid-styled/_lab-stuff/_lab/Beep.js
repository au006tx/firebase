import styled from 'glamorous'
import { space, fontSize, width, color } from 'styled-system'

const Beep = styled('div')(props => ({
  fontWeight: 600
}),
  space,
  fontSize,
  width,
  color
)

Beep.defaultProps = {
  color: 'blue',
  m: 0,
  fontSize: 8,
  pl: null
}

export default Beep