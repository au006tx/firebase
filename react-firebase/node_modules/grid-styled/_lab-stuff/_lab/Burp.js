import styled from 'glamorous'
import { space, fontSize, width, color } from 'styled-system'

const Burp = styled('div')(props => ({}),
  space,
  fontSize,
  width,
  color
)

Burp.defaultProps = {}

export default Burp