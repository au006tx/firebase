import styled from 'glamorous'
import { space, fontSize, width, color } from 'styled-system'

const Bloop = styled('div')(props => ({}),
  space,
  fontSize,
  width,
  color
)

Bloop.defaultProps = {
  fontSize: 5
}

export default Bloop