import styled from 'glamorous'
import { space, fontSize, width, color } from 'styled-system'

const Hello = styled('div')(props => ({
  color: `${props.theme('colors.blue')}`,
  borderRadius: null
}),
  space,
  fontSize,
  width,
  color
)

Hello.defaultProps = {
  fontSize: 4
}

export default Hello