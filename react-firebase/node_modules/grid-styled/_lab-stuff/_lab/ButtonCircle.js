import styled from 'glamorous'
import { space, fontSize, width, color } from 'styled-system'

const ButtonCircle = styled('button')(props => ({
  display: 'inline-block',
  fontFamily: 'inherit',
  borderRadius: '9999px',
  appearance: 'none',
  borderWidth: '0px',
  borderStyle: 'solid',
  fontWeight: 'bold',
  textTransform: 'uppercase',
  letterSpacing: '0.1875em'
}),
  space,
  fontSize,
  width,
  color
)

ButtonCircle.defaultProps = {
  fontSize: 1,
  px: 3,
  py: 3,
  color: 'black',
  bg: 'seafoam'
}

export default ButtonCircle